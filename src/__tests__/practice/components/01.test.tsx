import { render, screen } from '@testing-library/react'

import Button01 from '../../../components/01'

test('第一個測試', () => {
  // 1. render 出要測試的元件
  // 2. 在畫面上抓到該元件
  // 4. 檢查結果是否和期待相符

})

test('快照', () => {
  // 1. 2. render 出要測試的元件 && 在畫面上抓到該元件
  // 4. 檢查結果是否和期待相符
})
