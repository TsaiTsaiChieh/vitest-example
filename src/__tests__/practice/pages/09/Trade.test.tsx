import { render, screen } from '@testing-library/react'

import Trade from '../../../../pages/09/Trade'

// Given

beforeEach(() => {
  // mock random
})
describe('<Trade />', () => {
  test('spyOn', () => {
    // When
    // Expect
  })

  test('style', () => {
    // When
    // Expect
  })
})
